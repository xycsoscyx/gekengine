<?xml version="1.0"?>
<shader priority="2">
    <material>
        <transparent>
            <albedo bind="float4" pattern="color" parameters="(1,1,1,1)">Texture2D</albedo>
        </transparent>
    </material>

    <textures>
        <accumulationBuffer bind="float4" flags="target">half4</accumulationBuffer>
        <modulationDiffusionBuffer bind="float4" flags="target">byte4</modulationDiffusionBuffer>
        <refractionBuffer bind="float2" flags="target">half2</refractionBuffer>
        <depthBuffer bind="float">float</depthBuffer>
        <finalBuffer bind="float" source="standard" name="finalBuffer" />
        <sourceBuffer bind="float3">half4</sourceBuffer>
    </textures>

    <depth source="standard" />

    <block>
        <clear>
            <accumulationBuffer>0</accumulationBuffer>
            <modulationDiffusionBuffer>(1,1,1,0)</modulationDiffusionBuffer>
            <refractionBuffer>0</refractionBuffer>
        </clear>

        <pass mode="forward" name="transparent">
            <blendstates unified="false">
                <accumulationBuffer>
                    <color source="one" destination="one" operation="add" />
                    <alpha source="one" destination="one" operation="add" />
                </accumulationBuffer>
                <modulationDiffusionBuffer>
                    <color source="zero" destination="inverse_source_color" operation="add" />
                    <alpha source="one" destination="one" operation="add" />
                </modulationDiffusionBuffer>
                <refractionBuffer>
                    <color source="one" destination="one" operation="add" />
                    <alpha source="one" destination="one" operation="add" />
                </refractionBuffer>
            </blendstates>

            <renderstates>
                <cullmode>back</cullmode>
            </renderstates>

            <depthstates>
                <comparison>less</comparison>
                <writemask>zero</writemask>
            </depthstates>

            <targets>
                <accumulationBuffer />
                <modulationDiffusionBuffer />
                <refractionBuffer />
            </targets>

            <resources>
                <depthBuffer copy="depth" />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Transparent\Calculate</source>
            </program>
        </pass>

        <pass mode="deferred">
            <resources>
                <accumulationBuffer />
                <modulationDiffusionBuffer />
                <refractionBuffer />
                <sourceBuffer copy="finalBuffer" />
            </resources>

            <targets>
                <finalBuffer />
            </targets>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Transparent\Combine</source>
            </program>
        </pass>
    </block>
</shader>
