<?xml version="1.0"?>
<shader>
    <material>
        <maps>
            <albedo bind="float4" default="*color:1,0,1,1">Texture2D</albedo>
            <normal bind="float2" default="*normal:0,0,1">Texture2D</normal>
            <roughness bind="float" default="*color:.25">Texture2D</roughness>
            <metalness bind="float" default="*color:0">Texture2D</metalness>
        </maps>
    </material>

    <defines>
        <tileSize>32</tileSize>
        <dispatchWidth>ceil(%displayWidth% / %tileSize%)</dispatchWidth>
		<dispatchHeight>ceil(%displayHeight% / %tileSize%)</dispatchHeight>
    </defines>

    <depth>D32</depth>

    <targets>
        <albedoBuffer bind="float3">byte4</albedoBuffer>
        <materialBuffer bind="float2">byte2</materialBuffer>
        <normalBuffer bind="half2">half2</normalBuffer>
        <depthBuffer bind="float">float</depthBuffer>
        <luminatedBuffer bind="float3">float4</luminatedBuffer>
        <luminanceBuffer bind="float" flags="mipmaps">float</luminanceBuffer>
    </targets>

    <buffers>
        <tileIndexList size="%dispatchWidth% * %dispatchHeight% * %lightListSize%">byte</tileIndexList>
    </buffers>

    <block>
        <pass mode="forward">
            <depthstates>
                <comparison>lessequal</comparison>
                <writemask>all</writemask>
                <clear>1</clear>
            </depthstates>

            <renderstates>
                <cullmode>back</cullmode>
            </renderstates>

            <targets>
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </targets>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Deferred</source>
            </program>
        </pass>
    </block>

    <block>
        <pass mode="deferred">
            <targets>
                <luminatedBuffer />
            </targets>

            <resources>
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Ambient</source>
            </program>
        </pass>
    </block>

    <block lighting="true">
        <pass mode="compute">
            <resources>
                <depthBuffer />
            </resources>

            <unorderedaccess>
                <tileIndexList />
            </unorderedaccess>

            <program>
                <compute>
                    <width>%dispatchWidth%</width>
                    <height>%dispatchHeight%</height>
                    <depth>1</depth>
                </compute>
                <entry>mainComputeProgram</entry>
                <source>Standard\Tiled</source>
            </program>
        </pass>

        <pass mode="deferred">
            <blendstates>
                <color source="one" destination="one" operation="add" />
                <writemask>rgb</writemask>
            </blendstates>

            <targets>
                <luminatedBuffer />
            </targets>

            <resources>
                <tileIndexList />
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Lighting</source>
            </program>
        </pass>
    </block>

    <block>
        <pass mode="deferred">
            <resources>
                <luminatedBuffer />
            </resources>

            <targets>
                <luminanceBuffer />
            </targets>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Luminance</source>
            </program>
        </pass>
    </block>

    <block>
        <pass mode="deferred">
            <resources>
                <luminatedBuffer />
                <luminanceBuffer generateMipMaps="true" />
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\ToneMap</source>
            </program>
        </pass>
    </block>
</shader>
