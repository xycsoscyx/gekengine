<?xml version="1.0"?>
<shader>
    <material>
        <maps>
            <albedo bind="float4" loadFlags="">Texture2D</albedo>
            <normal bind="float2">Texture2D</normal>
            <roughness bind="float">Texture2D</roughness>
            <metalness bind="float">Texture2D</metalness>
        </maps>
    </material>

    <defines>
        <tileSize>32</tileSize>
        <dispatchWidth>ceil(%displayWidth% / %tileSize%)</dispatchWidth>
		<dispatchHeight>ceil(%displayHeight% / %tileSize%)</dispatchHeight>
    </defines>

    <depth>D32</depth>

    <targets>
        <albedoBuffer bind="float3">byte4</albedoBuffer>
        <materialBuffer bind="float2">byte2</materialBuffer>
        <normalBuffer bind="half2">half2</normalBuffer>
        <depthBuffer bind="float">float</depthBuffer>
        <compositeBuffer bind="float4" flags="mipmaps">float4</compositeBuffer>
    </targets>

    <buffers>
        <tileIndexList size="%dispatchWidth% * %dispatchHeight% * %lightListSize%">byte</tileIndexList>
    </buffers>

    <block>
        <pass mode="forward">
            <depthstates>
                <comparison>lessequal</comparison>
                <writemask>all</writemask>
                <clear>1</clear>
            </depthstates>

            <renderstates>
                <cullmode>back</cullmode>
            </renderstates>

            <targets>
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </targets>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Deferred</source>
            </program>
        </pass>
    </block>

    <block lighting="true">
        <clear>
            <compositeBuffer>0,0,0,0</compositeBuffer>
        </clear>

        <pass mode="compute">
            <resources>
                <depthBuffer />
            </resources>

            <unorderedaccess>
                <tileIndexList />
            </unorderedaccess>

            <program>
                <compute>
                    <width>%dispatchWidth%</width>
                    <height>%dispatchHeight%</height>
                    <depth>1</depth>
                </compute>
                <entry>mainComputeProgram</entry>
                <source>Standard\Tiled</source>
            </program>
        </pass>

        <pass mode="deferred">
            <blendstates>
                <color source="one" destination="one" operation="add" />
                <alpha source="one" destination="one" operation="add" />
            </blendstates>

            <targets>
                <compositeBuffer />
            </targets>

            <resources>
                <tileIndexList />
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\Lighting</source>
            </program>
        </pass>
    </block>

    <block>
        <pass mode="deferred">
            <resources>
                <compositeBuffer generateMipMaps="true" />
                <albedoBuffer />
                <materialBuffer />
                <normalBuffer />
                <depthBuffer />
            </resources>

            <program>
                <entry>mainPixelProgram</entry>
                <source>Standard\ToneMap</source>
            </program>
        </pass>
    </block>
</shader>
