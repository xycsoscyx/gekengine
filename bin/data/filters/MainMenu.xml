<?xml version="1.0"?>
<filter>
    <textures>
        <texture data="*browser:mainmenu" />
    </textures>
    <blend>
        <color source="one" destination="one" operation="add" />
        <alpha source="one" destination="one" operation="add" />
    </blend>
    <shader>
        <![CDATA[
            Texture2D     gs_pBrowser               : register(t0);

            float4 MainPixelProgram(INPUT kInput) : SV_TARGET
            {
	            return gs_pBrowser.Sample(gs_pPointSampler, kInput.texcoord);
            }
        ]]>
    </shader>
</filter>
