<?xml version="1.0"?>
<filter mode="forward">
    <states cullmode="back" />
    
    <targets>
        <depth name="Opaque.DepthBuffer" format="D24S8" comparison="less" writemask="all" clear="0" />
    </targets>
    
    <pixel>
        <![CDATA[
            Texture2D     gs_pAlbedoMap             : register(t0);

            float4 MainPixelProgram(in INPUT kInput) : SV_TARGET0
            {
                return (gs_pAlbedoMap.Sample(gs_pLinearSampler, kInput.texcoord) * kInput.color);
            }
        ]]>
    </pixel>
</filter>
